# csrf-newbie

```
# Install
poetry install

# Run app
FLASK_APP=src.app poetry run flask run -p 8000 --debugger --reload
# Run evil app
FLASK_APP=src.evil poetry run flask run -p 9000 --debugger --reload
```
